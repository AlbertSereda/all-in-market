apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "grafana.configMapDatasources" . }}
  labels:
    {{- include "grafana.labels" . | nindent 4 }}
data:
{{- range $key, $value := .Values.datasourcesConfig }}
  {{ $key }}: |
{{ $value | nindent 4 }}
{{- end }}