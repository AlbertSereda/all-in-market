apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "grafana.configMapJvmDashboard" . }}
  labels:
    {{- include "grafana.labels" . | nindent 4 }}
data:
{{- range $key, $value := .Values.jvmDashboardConfig }}
  {{ $key }}: |
{{ $value | nindent 4 }}
{{- end }}