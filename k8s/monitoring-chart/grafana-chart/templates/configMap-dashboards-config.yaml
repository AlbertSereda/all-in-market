apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "grafana.configMapDashboard" . }}
  labels:
    {{- include "grafana.labels" . | nindent 4 }}
data:
{{- range $key, $value := .Values.dashboardsConfig }}
  {{ $key }}: |
{{ $value | nindent 2 }}
{{- end }}